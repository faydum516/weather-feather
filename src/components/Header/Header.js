import './Header.css';

function Header() {
    return (
        <header className="Header">
            <h2 className="HeaderTitle">Weather Feather</h2>
        </header>
    );
}

export default Header;